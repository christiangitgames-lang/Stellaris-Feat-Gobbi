pop_resettlement_cost = {
	# Cost = (1 + election_cost_mult) * empire_size * 10
	base = 1
	add = modifier:pop_resettlement_cost_mult
	min = 0.01
	# Rounded to closest 10, by applying x10 last.
}
country_resettlement_cost = {
	base = 1
	add = modifier:country_resettlement_cost_mult
}
value_country_resettlement_cost = {
	base = 1
	add = modifier:country_resettlement_cost_mult
	mult = 500
}
planet_building_refund_mult = {
	base = 0
	add = modifier:planet_building_refund_mult
	min = 0
	max = 1
}
starbases_upgrading_cap_trigger = {
	base = 0
	complex_trigger_modifier = {
		trigger = count_owned_starbase
		trigger_scope = owner
		parameters = {
			limit = {
				has_starbase_size = starbase_outpost
				has_starbase_flag = auto_upgrading_starbase_flag
			}
		}
		mode = add
	}
	add = trigger:used_starbase_capacity_integer
	add = 1 #1 rimangono smepre vuote
	min = 0
}
negative_modifier_mult = {
	base = -1
	subtract = modifier:$MODIFIER$
	# modifier = { #is_machine_empire starbase 50% cost
	# 	subtract = 0.5
	# 	is_machine_empire = yes
	# }
	max = -0.1
}

positive_modifier_mult = {
	base = 1
	add = modifier:$MODIFIER$
	# modifier = { #is_machine_empire starbase 50% cost
	# 	add = 0.5
	# 	is_machine_empire = yes
	# }
	min = 0.1
}